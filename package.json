{
  "name": "eslint-config-js",
  "version": "1.0.0",
  "description": "how to configure eslint for JavaScript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx standard",
    "lint-fix": "npx standard --fix"
  },
  "license": "ISC",
  "devDependencies": {
    "standard": "^17.0.0"
  },
  "eslintConfig": {
    "extends": [ "standard" ]
  }
}
